# Codegen SDK Configuration
# Get these values from https://codegen.com/developer

# Required: Your organization ID
CODEGEN_ORG_ID=your-org-id-here

# Required: Your API token
CODEGEN_TOKEN=your-api-token-here

# Optional: Custom API base URL (defaults to production)
# CODEGEN_BASE_URL=https://codegen-sh-rest-api.modal.run

# Optional: Request timeout in milliseconds (default: 30000)
# CODEGEN_TIMEOUT=30000

# Optional: Number of retry attempts (default: 3)
# CODEGEN_RETRIES=3

# Optional: Maximum concurrent tasks (default: 5)
# CODEGEN_MAX_CONCURRENT_TASKS=5

# Optional: Task timeout in milliseconds (default: 600000)
# CODEGEN_TASK_TIMEOUT=600000

# Optional: Enable event emission (default: true)
# CODEGEN_ENABLE_EVENTS=true

# Optional: Enable automatic retry for failed tasks (default: true)
# CODEGEN_ENABLE_AUTO_RETRY=true

