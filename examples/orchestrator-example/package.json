{
  "name": "voltagent-orchestrator-example",
  "version": "0.1.0",
  "private": true,
  "description": "VoltAgent example demonstrating the orchestration system for system integration and end-to-end workflows.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "build": "tsup",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@voltagent/core": "workspace:*",
    "@voltagent/vercel-ai": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.2"
  }
}

